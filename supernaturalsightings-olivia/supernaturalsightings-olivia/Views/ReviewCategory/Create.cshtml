@model supernaturalsightings_olivia.Models.ReviewCategory

    <h1>All Review Categories</h1>
@*
    @if (Model.Count == 0)
{
        <p> No Review Categories yet </p>
    }
    else
{*@
    <form asp-controller="ReviewCategory" asp-action="ProcessAddReviewForm" method="post">
        <div class="form-group">
            <label asp-for="Username"></label>
            <input class="form-control" asp-for="Username" />
            <span asp-validation-for="Username"></span>
        </div>

    <div class="form-group">
        <label asp-for="ReviewType"></label>
        <input class="form-control" asp-for="ReviewType" />
        <span asp-validation-for="ReviewType"></span>
    </div>
    </form>
        @*<table class= "table">
            <tr>
                <th> Category Name </ th>
            </tr>

            @foreach(ReviewCategory category in Model)
            {
                <tr>
                    <td> @category.ReviewType </ td>
                </tr>
            
        </table>
            }*@
}