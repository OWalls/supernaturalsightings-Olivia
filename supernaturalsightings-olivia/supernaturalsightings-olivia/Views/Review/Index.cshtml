@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}

@model List<supernaturalsightings_olivia.Models.Review>
@using Microsoft.AspNetCore.Identity

@inject SignInManager<IdentityUser> SignInManager
@inject UserManager<IdentityUser> UserManager

<p>
    <a asp-controller="Review" asp-action="AddReview">Add Review</a>
</p>
<h2>@ViewData["Title"]</h2>


    <table class="table">
        <thead>
            <tr>
            <th>Username</th>
            <th>Review Title</th>
            <th>Review Comment</th>
            </tr>
        </thead>

        <tbody>
            @if (Model is not null)
            {
                @foreach (Review r in Model)
                {
                    <tr>
                        <td>
                            @r.Username
                        </td>
                    <td>
                        @r.ReviewTitle
                    </td>
                    <td>
                        @r.ReviewComment
                    </td>
                    </tr>
            @*        <tr>
                        <td>
                            @Html.DisplayFor(modelItem => r.Username)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => r.ReviewTitle)
                        </td>
                    <td>
                        @Html.DisplayFor(modelItem => r.ReviewComment)
                    </td>
                    </tr>*@
                }
            }
        </tbody>
    </table>